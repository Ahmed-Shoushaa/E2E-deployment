version: '3'

services:
  db:
    build: ./db
    ports:
      - "30001:3306"

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

  app:
    build: ./app
    ports:
      - "5000:5000"
    links:
      - db